server:
  port: 8081  # 后端项目端口
  servlet:
    context-path: /api  # 接口统一前缀

spring:
  profiles:
    active: dev  # 开发环境（保留这行）
    # active: prod  # 生产环境（注释掉，需要时再打开）

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/shopping_db?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Jw52629043826697  # 你的MySQL密码

  redis:
    host: localhost
    port: 6379
    password:
    database: 0
    connect-timeout: 10s
    timeout: 10s
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms

mybatis:
  mapper-locations: classpath:mapper/*.xml
  # type-aliases-package: com.geng.demo.mybatis.entity  # 注释要加空格，且缩进对齐